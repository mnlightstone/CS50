#include <cs50.h>
#include <stdio.h>
#include <string.h>

// #include "helpers.h"
// #include "wav.h"

int duration(string fraction)
    {
    //divide fraction[0] by fracton[2]
    //divide total above by 1/8
    //return total
    printf("%s",fraction);
    // int total = atoi(fraction[0])/atoi(fraction[2]);
    // total = total / (1/8);
    // return total;
    return 1;
}